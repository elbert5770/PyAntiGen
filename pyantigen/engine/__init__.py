"""Simulation, fitting, profiling and identifiability.

Was copied into every project as ``Projects/<name>/Engine`` in 1.x; since 2.0
it is imported from the installed package so all projects share one version.
Nothing in here may import project code (``Modules``, ``AntiGen_paths``).
"""
